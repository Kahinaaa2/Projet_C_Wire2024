
Proposition de découpage

    Fichier main.c :
        Contient la logique principale du programme : la gestion de l'entrée et de la sortie, et les appels aux fonctions définies

    Fichier avl.h :
        Contient les définitions de la structure AVL et les prototypes des fonctions liées à la gestion de l'arbre AVL.

    Fichier avl.c :
        Contient les implémentations des fonctions liées à l'arbre AVL.

    Fichier utils.h :
        Contient les fonctions comme min et max.

    Fichier utils.c :
        Implémente les fonctions déclarées dans utils.h.








projet/
├── Makefile
├── main.c
├── avl.c
├── avl.h
├── utils.c
├── utils.h





# Nom du compilateur
CC = gcc

# Options de compilation
CFLAGS = -Wall -Wextra -g

# Fichiers sources
SRC = main.c avl.c utils.c

# Fichiers objets
OBJ = $(SRC:.c=.o)

# Nom de l'exécutable
TARGET = avl_programme

# Règle par défaut
all: $(TARGET)

# Compilation de l'exécutable
$(TARGET): $(OBJ)
	$(CC) $(CFLAGS) -o $@ $^

# Compilation des fichiers .c en fichiers .o
%.o: %.c
	$(CC) $(CFLAGS) -c $<

# Nettoyage des fichiers générés
clean:
	rm -f $(OBJ) $(TARGET)

# Nettoyage approfondi
mrproper: clean
	rm -f *~
